# Application Configuration
app.url=https://animated-gingersnap-8cf7f2.netlify.app/

# Browser Configuration
browser=chrome
headless=false

# Timeouts (in seconds)
implicit.wait=10
explicit.wait=20
page.load.timeout=30

# Credentials
login.email=admin
login.password=password123

# Test Configuration
take.screenshot.on.failure=true